{
  "name": "@artevelde-uas/canvas-lms-sprintplus-accessibility-toggle-plugin",
  "version": "0.1.1",
  "description": "Adds a toggle switch to the user's accessibility settings in Canvas LMS for enabling/disabling the Sprint+ Websprinter UI.",
  "keywords": [
    "Canvas LMS",
    "Accessibility",
    "Sprint+",
    "Websprinter"
  ],
  "license": "ISC",
  "author": "Renaat De Muynck <renaat.demuynck@arteveldehs.be>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/artevelde-uas/canvas-lms-sprintplus-accessibility-toggle-plugin.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "module": "src/index.js",
  "files": [
    "src/",
    "locales/"
  ],
  "scripts": {
    "dev": "run-p serve watch",
    "watch": "cross-env NODE_OPTIONS=--openssl-legacy-provider webpack ./index.js --watch --progress --stats minimal --config webpack.config.js",
    "serve": "canvas-server-start"
  },
  "packageManager": "yarn@4.18.0",
  "engines": {
    "node": ">=18.20.8 <21"
  },
  "peerDependencies": {
    "@artevelde-uas/canvas-lms-app": "^1.14.0"
  },
  "devDependencies": {
    "@artevelde-uas/canvas-lms-app": "1.15.2",
    "@artevelde-uas/canvas-lms-theme-dev": "0.4.3",
    "cross-env": "^10.1.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "p-timeout": "5.1.0"
  }
}
